int maxDepth(TreeNode* root) {
        // write code here
        if(root!=nullptr)
        {
            int l=maxDepth(root->left);
            int r=maxDepth(root->right);
            return max(l, r)+1;
        }
        return 0;
    }