 ListNode* ReverseList(ListNode* head) {
        // write code here
        if(head==nullptr)return head;
        ListNode*prev=nullptr,*cur=head;
        while(cur!=nullptr)
        {
          head=head->next;
          cur->next=prev;
          prev=cur;
          cur=head;
        }
        return prev;
    }
